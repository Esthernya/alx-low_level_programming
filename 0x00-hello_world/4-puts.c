#include <stdio.h>#include <stdio.h>
/**
 *  * main should print out a string at entry.
 *   * Return: 0 if success.
 *    */
int main(void)
{
		puts("\"Programming is like building a multilingual puzzle");
			return (0);
}
int main(void)
{
		puts("\"Programming is like building a multilingual puzzle");
			return (0);
}
